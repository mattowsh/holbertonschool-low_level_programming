#include <stdio.h>

/**
 * main: is the main function in this C script, prints "Programming is like building a multilingual puzzle" and return PID = errors quantity
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
